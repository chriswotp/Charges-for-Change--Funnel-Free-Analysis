# CLAUDE.md — AI Assistant Guide

## Project Overview

**Charges for Change: Funnel-Free Analysis** is a repository for analyzing charge/donation flows without traditional funnel constraints. The project is in its initial state and awaiting implementation.

## Repository Status

This repository is currently **empty** (no source code, configs, or documentation beyond this file). All structure described below should be established as the project develops.

## Development Branch

- **Current active branch:** `claude/claude-md-docs-KQ1iW`
- **Remote:** `origin` (GitHub — `chriswotp/Charges-for-Change--Funnel-Free-Analysis`)
- **Other branch:** `claude/add-claude-documentation-G4kSd` (both branches point to the same initial commit)

## Getting Started

Since the project has no code yet, contributors should:

1. Define the tech stack and create appropriate config files (e.g., `package.json`, `requirements.txt`)
2. Add a `README.md` with project goals, setup instructions, and usage
3. Establish a directory structure matching the chosen architecture
4. Set up CI/CD workflows (e.g., `.github/workflows/`)

## Conventions for AI Assistants

### General Rules

- Read existing files before proposing changes — never guess at file contents.
- Keep changes minimal and focused on what was requested.
- Do not add unnecessary abstractions, comments, or documentation beyond what is needed.
- Prefer editing existing files over creating new ones.

### Git Workflow

- Commit messages should be concise and describe the "why" of a change.
- Always push to the designated feature branch, never directly to `main`.
- Stage specific files rather than using `git add -A`.

### Code Quality

- Follow the conventions already present in the codebase once code exists.
- Do not introduce known security vulnerabilities (OWASP Top 10).
- Avoid over-engineering — implement only what is explicitly requested.

## File Structure (To Be Established)

```
Charges-for-Change--Funnel-Free-Analysis/
├── CLAUDE.md          # This file — AI assistant guide
├── README.md          # Project overview and setup (to be created)
├── src/               # Source code (to be created)
├── tests/             # Test suite (to be created)
└── docs/              # Additional documentation (to be created)
```

## Build / Test / Lint Commands

No commands configured yet. Update this section as tooling is added.

## Dependencies

No dependencies configured yet. Update this section as the project takes shape.
